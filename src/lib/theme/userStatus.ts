export const USER_STATUS_LABEL: Record<UserStatus, string> = {
  available: "Available",
  away: "Away",
};

/** Solid fill — status badges and user avatars. */
export const USER_STATUS_BG: Record<UserStatus, string> = {
  available: "bg-success",
  away: "bg-danger",
};
